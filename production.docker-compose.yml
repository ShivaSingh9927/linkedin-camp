# version: '3.8'

# services:
#   backend:
#     build: 
#       context: .
#       dockerfile: Dockerfile
#     restart: always
#     environment:
#       - DATABASE_URL=postgresql://shiva:password123@db:5432/linkedin_camp
#       - REDIS_URL=redis://redis:6379
#       - JWT_SECRET=Iamironman@3
#       - PORT=3001
#       - NODE_ENV=production
#       - BROWSERLESS_HOST=browserless
#       - BROWSERLESS_PORT=3000
#       - BROWSERLESS_TOKEN=Raja_Security_2026
#     ports:
#       - "3001:3001"
#     depends_on:
#       - db
#       - redis
#       - browserless
#     volumes:
#       - /root/linkedin-sessions:/app/sessions
#     networks:
#       - linkedin-network

# version: '3'
# services:
#   browserless:
#     image: browserless/browserless:latest  # Changed from /chrome to /browserless
#     restart: always
#     ports:
#       - "3000:3000"
#     environment:
#       - MAX_CONCURRENT_SESSIONS=5
#       - TOKEN=Raja_Security_2026
#       - WORKSPACE_DIR=/sessions             # Variable name fix
#       - ENABLE_DEBUGGER=true
#       - ENABLE_CORS=true                    # Variable name fix
#       - PREBOOT_CHROME=true
#     volumes:
#       - /root/linkedin-sessions:/sessions
#     networks:
#       - linkedin-network

# networks:
#   linkedin-network:
#     driver: bridge
#   db:
#     image: postgres:15-alpine
#     restart: always
#     environment:
#       - POSTGRES_USER=shiva
#       - POSTGRES_PASSWORD=password123
#       - POSTGRES_DB=linkedin_camp
#     volumes:
#       - pg_data:/var/lib/postgresql/data
#     networks:
#       - linkedin-network

#   redis:
#     image: redis:7-alpine
#     restart: always
#     networks:
#       - linkedin-network

# networks:
#   linkedin-network:
#     driver: bridge

# volumes:
#   pg_data:

version: '3.8'

services:
  backend:
    build: .
    restart: always
    environment:
      - DATABASE_URL=postgresql://shiva:password123@db:5432/linkedin_camp
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=Iamironman@3
      - PORT=3001
      - NODE_ENV=production
      - AI_SERVICE_URL=http://ai-service:8001
      - GROQ_API=${GROQ_API}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - OUTLOOK_EMAIL=${OUTLOOK_EMAIL}
      - OUTLOOK_APP_PASSWORD=${OUTLOOK_APP_PASSWORD}
      - NEXT_PUBLIC_APP_URL=https://linkedin-camp-web.vercel.app
    ports:
      - "3001:3001"
    depends_on:
      - db
      - redis
    volumes:
      - /root/linkedin-sessions:/app/sessions
    networks:
      - linkedin-network

  db:
    image: postgres:15-alpine
    restart: always
    environment:
      - POSTGRES_USER=shiva
      - POSTGRES_PASSWORD=password123
      - POSTGRES_DB=linkedin_camp
    volumes:
      - pg_data:/var/lib/postgresql/data
    networks:
      - linkedin-network

  redis:
    image: redis:7-alpine
    restart: always
    networks:
      - linkedin-network

  ai-service:
    build: 
      context: ./apps/ai-service
    restart: always
    environment:
      - GROQ_API=${GROQ_API}
    networks:
      - linkedin-network

networks:
  linkedin-network:
    driver: bridge

volumes:
  pg_data:
